import { ListaGruposAssignadosPipePipe } from './lista-grupos-assignados-pipe.pipe';

describe('ListaGruposAssignadosPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ListaGruposAssignadosPipePipe();
    expect(pipe).toBeTruthy();
  });
});
